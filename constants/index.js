export const contractAddresses = require("./contract_addresses");
export const contractAbi = require("./contract_abi");
export const connectedHandler = data => console.log("CONNECTED", data);
export const disconnectedHandler = data => console.log("DISCONNECTED", data);
